# Variáveis de Ambiente do Projeto de E-commerce

# --- Geral ---
NODE_ENV=development
# O ambiente em que a aplicação está rodando (development, production, test)

# --- API Backend ---
# A URL base da sua API de backend
VITE_API_BASE_URL=http://localhost:8000/api/ # Para desenvolvimento (se usando Vite)
# Ou:
# REACT_APP_API_BASE_URL=http://localhost:3000/api # Para Create React App

# --- Chaves de API (Exemplos) ---
# Se você usar algum serviço de terceiros (ex: Stripe para pagamentos, Cloudinary para imagens)
VITE_STRIPE_PUBLIC_KEY=pk_test_SEUA_CHAVE_PUBLICA_AQUI # Chave pública do Stripe
# REACT_APP_STRIPE_PUBLIC_KEY=pk_test_SEUA_CHAVE_PUBLICA_AQUI

# VITE_CLOUDINARY_CLOUD_NAME=seu_cloud_name
# REACT_APP_CLOUDINARY_CLOUD_NAME=seu_cloud_name

# --- Outras configurações (Exemplos) ---
# Algum limite de itens por página padrão
VITE_DEFAULT_ITEMS_PER_PAGE=10
# REACT_APP_DEFAULT_ITEMS_PER_PAGE=10

# Chave secreta para JWT (apenas para o backend, se você tiver um)
# Se você tiver um backend Node.js/Express, esta é uma variável de ambiente do lado do servidor
# JWT_SECRET_KEY=sua_chave_secreta_jwt_longa_e_aleatoria

# Credenciais de banco de dados (também apenas para o backend)
DB_HOST=mysql
DB_PORT=3306
DB_USER=sail
DB_PASSWORD=password
DB_NAME=laravel